<html>
  <head>
    <title>Editer un potin</title>
  </head>
  <body>
    <h1>Edite un potin !</h1>
    <p>Modifie le potin via le formulaire ci-dessous</p>
      <% gossips.each_with_index do |gossip, index| %>
    <form action='/gossips/<%= (index +1)%>/edit/' method='POST'>
      Saisi ici le nom modifié :  <input type='text' name='gossip_author'/><br/>
      Balance le potin modifié : <input type='text' name='gossip_content'/><br/>
      <input type='submit'/>
      <% end %>
    </form>
  </body>
</html>